# 项目初始化指南 - 一键启动开发

## 快速开始（5 分钟）

### Step 1：安装中文化 Claude Code 配置

```bash
# 克隆您的中文化配置仓库
git clone https://github.com/Carlehyy/benchmark-claude-code.git

# 进入目录
cd benchmark-claude-code

# 安装到用户级配置（推荐）
mkdir -p ~/.claude
cp -r agents ~/.claude/
cp -r commands ~/.claude/
cp -r skills ~/.claude/
cp -r rules ~/.claude/
cp -r contexts ~/.claude/

# 验证安装
# 在 Claude Code 中输入 /help 查看所有命令
```

### Step 2：创建项目

```bash
# 创建项目目录
mkdir logic-expression-miniapp
cd logic-expression-miniapp

# 初始化 Git
git init

# 创建基础目录结构
mkdir -p miniprogram/{pages,components,services,models,utils,config}
mkdir -p cloudfunctions
mkdir -p tests/{unit,integration,e2e}
mkdir -p docs
mkdir -p .claude
```

### Step 3：初始化微信小程序

#### 方式一：使用微信开发者工具（推荐）

1. 打开微信开发者工具
2. 点击"新建项目"
3. 选择项目目录：`logic-expression-miniapp`
4. 填写 AppID（测试号也可以）
5. 选择"不使用云服务"（稍后手动配置）
6. 点击"新建"

#### 方式二：使用命令行

```bash
# 安装微信开发者工具命令行
npm install -g miniprogram-cli

# 创建项目
miniprogram-cli create --name logic-expression-miniapp --template default
```

### Step 4：配置 TypeScript

创建 `tsconfig.json`：

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "CommonJS",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "moduleResolution": "node",
    "resolveJsonModule": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["miniprogram/*"]
    },
    "types": ["miniprogram-api-typings"]
  },
  "include": ["miniprogram/**/*"],
  "exclude": ["node_modules"]
}
```

安装类型定义：

```bash
npm install --save-dev typescript miniprogram-api-typings
```

### Step 5：配置 Claude Code 项目规则

创建 `.claude/CLAUDE.md`：

```markdown
# 逻辑表达力小程序 - Claude Code 配置

## 项目信息

**项目名称**：逻辑表达力训练小程序  
**技术栈**：微信小程序原生 + TypeScript + 云开发  
**开发模式**：TDD（测试驱动开发）  
**目标**：帮助用户提升逻辑思维和表达能力

## 代码规范

### 命名规范
- 文件名：kebab-case（question-list.ts）
- 类名：PascalCase（QuestionService）
- 函数名：camelCase（getQuestions）
- 常量：UPPER_SNAKE_CASE（MAX_QUESTIONS）

### TypeScript 规范
- 所有函数必须有类型注解
- 禁止使用 any 类型
- 接口命名以 I 开头或不加前缀
- 使用 interface 而非 type（除非必要）

### 注释规范
- 所有公共函数必须有 JSDoc 注释
- 复杂逻辑必须有行内注释
- 注释使用中文

### 示例
\`\`\`typescript
/**
 * 获取题目列表
 * @param difficulty 难度筛选
 * @param category 分类筛选
 * @returns 题目列表
 */
export async function getQuestions(
  difficulty?: Difficulty,
  category?: string
): Promise<Question[]> {
  // 实现代码
}
\`\`\`

## 测试要求

### 覆盖率要求
- 单元测试覆盖率 > 80%
- 核心业务逻辑覆盖率 > 90%
- 所有公共 API 必须有测试

### 测试命名
- 测试文件：`*.test.ts` 或 `*.spec.ts`
- 测试描述：使用中文描述
- 测试结构：AAA（Arrange-Act-Assert）

### 示例
\`\`\`typescript
describe('QuestionService', () => {
  describe('getQuestions', () => {
    it('应该返回所有题目', async () => {
      // Arrange
      const service = new QuestionService();
      
      // Act
      const questions = await service.getQuestions();
      
      // Assert
      expect(questions).toHaveLength(10);
    });
  });
});
\`\`\`

## Git 工作流

### 分支策略
- `main`：主分支，保护分支，只接受 PR
- `develop`：开发分支
- `feature/*`：功能分支
- `bugfix/*`：修复分支
- `hotfix/*`：紧急修复分支

### 提交规范
遵循 Conventional Commits 规范：

\`\`\`
<type>(<scope>): <subject>

<body>

<footer>
\`\`\`

**Type 类型**：
- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档更新
- `style`: 代码格式（不影响功能）
- `refactor`: 重构
- `test`: 测试相关
- `chore`: 构建/工具相关

**示例**：
\`\`\`
feat(question): 实现题库列表筛选功能

- 添加难度筛选
- 添加分类筛选
- 添加搜索功能

Closes #123
\`\`\`

### PR 流程
1. 从 `develop` 创建功能分支
2. 完成开发和测试
3. 运行 `/code-review` 审查代码
4. 提交 PR 到 `develop`
5. 通过 CI 检查
6. Code Review
7. 合并到 `develop`

## 强制规则

### 开发规则
- ✅ 所有功能必须使用 TDD 开发
- ✅ 提交前必须通过所有测试
- ✅ 提交前必须运行 `/code-review`
- ✅ 禁止直接提交到 `main` 分支

### 安全规则
- ✅ 禁止硬编码敏感信息（API Key、密码等）
- ✅ 所有用户输入必须验证
- ✅ 使用 HTTPS 传输数据
- ✅ 定期运行 `/security-review`

### 性能规则
- ✅ 图片必须压缩
- ✅ 列表使用虚拟滚动（超过 100 项）
- ✅ 避免频繁的 setData
- ✅ 使用防抖/节流处理高频事件

## 使用的 Skills

### 必须使用
- `tdd-workflow`：TDD 开发流程
- `coding-standards`：代码规范
- `security-review`：安全审查

### 推荐使用
- `continuous-learning`：持续学习和改进
- `verification-loop`：验证循环

## 使用的 Agents

### 规划阶段
- `planner`：创建实施计划
- `architect`：架构设计

### 开发阶段
- `tdd-guide`：TDD 指导
- `code-reviewer`：代码审查

### 测试阶段
- `e2e-runner`：E2E 测试

### 上线阶段
- `security-reviewer`：安全审查
- `doc-updater`：文档更新

## 常用命令

### 开发命令
- `/plan`：创建功能实施计划
- `/tdd`：TDD 开发指导
- `/code-review`：代码审查
- `/refactor-clean`：重构清理

### 测试命令
- `/e2e`：生成 E2E 测试
- `/test-coverage`：检查测试覆盖率

### 上线命令
- `/security-review`：安全审查
- `/update-docs`：更新文档

## 项目目标

### MVP 目标（3 周）
- ✅ 题库列表和详情
- ✅ 答题功能
- ✅ 学习进度追踪
- ✅ 基础数据分析

### 完整版目标（2 个月）
- ✅ 表达训练模块
- ✅ AI 智能评估
- ✅ 社交功能
- ✅ 高级数据分析

## 质量标准

### 代码质量
- 测试覆盖率 > 80%
- 无严重 ESLint 错误
- 通过代码审查

### 性能标准
- 首屏加载 < 2s
- 页面切换 < 300ms
- 包体积 < 2MB

### 安全标准
- 通过安全审查
- 无敏感信息泄露
- 用户隐私保护

---

**记住：始终使用 TDD，保持代码整洁，定期审查！** 🚀
\`\`\`

保存为 `.claude/CLAUDE.md`

## 完整的项目结构

运行以下脚本创建完整的项目结构：

```bash
#!/bin/bash

# 创建项目结构脚本
# 保存为 setup-project.sh，运行：bash setup-project.sh

echo "🚀 开始创建项目结构..."

# 创建目录
mkdir -p miniprogram/pages/{index,question-list,question-detail,practice,progress,profile}
mkdir -p miniprogram/components/{question-card,progress-chart,answer-input,ability-radar}
mkdir -p miniprogram/services
mkdir -p miniprogram/models
mkdir -p miniprogram/utils
mkdir -p miniprogram/config
mkdir -p miniprogram/assets/{images,icons}
mkdir -p cloudfunctions/{getQuestions,submitAnswer,evaluateLogic}
mkdir -p tests/{unit,integration,e2e}
mkdir -p docs
mkdir -p .claude

# 创建配置文件
cat > project.config.json << 'EOF'
{
  "description": "逻辑表达力训练小程序",
  "miniprogramRoot": "miniprogram/",
  "cloudfunctionRoot": "cloudfunctions/",
  "setting": {
    "urlCheck": true,
    "es6": true,
    "enhance": true,
    "postcss": true,
    "minified": true,
    "coverView": true
  },
  "compileType": "miniprogram",
  "appid": "your-appid",
  "projectname": "logic-expression-miniapp"
}
EOF

# 创建 TypeScript 配置
cat > tsconfig.json << 'EOF'
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "CommonJS",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "moduleResolution": "node",
    "resolveJsonModule": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["miniprogram/*"]
    },
    "types": ["miniprogram-api-typings"]
  },
  "include": ["miniprogram/**/*"],
  "exclude": ["node_modules"]
}
EOF

# 创建 package.json
cat > package.json << 'EOF'
{
  "name": "logic-expression-miniapp",
  "version": "1.0.0",
  "description": "逻辑表达力训练小程序",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "lint": "eslint miniprogram/**/*.ts",
    "lint:fix": "eslint miniprogram/**/*.ts --fix"
  },
  "keywords": ["miniprogram", "logic", "education"],
  "author": "Carlehyy",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "miniprogram-api-typings": "^3.0.0",
    "miniprogram-simulate": "^1.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  }
}
EOF

# 创建 README
cat > README.md << 'EOF'
# 逻辑表达力训练小程序

帮助用户提升逻辑思维和表达能力的微信小程序。

## 功能特性

- 📚 逻辑题库（推理题、论证题、谬误识别）
- ✍️ 表达训练（结构化表达、论证框架）
- 📊 学习进度追踪
- 📈 数据分析和可视化
- 🤖 AI 智能评估（可选）

## 技术栈

- 微信小程序原生开发
- TypeScript
- 云开发
- TDD 测试驱动开发

## 快速开始

详见 [项目初始化指南.md](docs/项目初始化指南.md)

## 开发计划

详见 [项目开发计划.md](docs/项目开发计划.md)

## License

MIT
EOF

# 创建 .gitignore
cat > .gitignore << 'EOF'
# 依赖
node_modules/

# 构建产物
dist/
build/

# 日志
*.log
npm-debug.log*

# 编辑器
.vscode/
.idea/
*.swp
*.swo

# 系统文件
.DS_Store
Thumbs.db

# 微信开发者工具
.miniprogram-cache/

# 测试覆盖率
coverage/

# 环境变量
.env
.env.local

# 临时文件
*.tmp
EOF

echo "✅ 项目结构创建完成！"
echo ""
echo "下一步："
echo "1. 安装依赖：npm install"
echo "2. 配置 AppID：编辑 project.config.json"
echo "3. 打开微信开发者工具"
echo "4. 开始开发：在 Claude Code 中运行 /plan"
```

保存为 `setup-project.sh`，然后运行：

```bash
bash setup-project.sh
```

## 安装依赖

```bash
# 安装项目依赖
npm install

# 安装开发工具
npm install -g miniprogram-cli
```

## 配置云开发

### 1. 在微信开发者工具中开通云开发

1. 打开项目
2. 点击"云开发"按钮
3. 开通云开发环境
4. 获取环境 ID

### 2. 配置云环境

创建 `miniprogram/config/cloud.config.ts`：

```typescript
export const cloudConfig = {
  env: 'your-env-id', // 替换为您的环境 ID
  traceUser: true
};
```

### 3. 初始化云开发

在 `miniprogram/app.ts` 中：

```typescript
import { cloudConfig } from './config/cloud.config';

App({
  onLaunch() {
    // 初始化云开发
    wx.cloud.init({
      env: cloudConfig.env,
      traceUser: cloudConfig.traceUser
    });
  }
});
```

## 验证安装

### 1. 检查 Claude Code 配置

在 Claude Code 中运行：

```
/help
```

应该能看到所有中文化的命令。

### 2. 检查项目结构

```bash
tree -L 2 -I 'node_modules'
```

应该看到完整的目录结构。

### 3. 运行测试

```bash
npm test
```

应该显示"No tests found"（因为还没有测试文件）。

### 4. 打开微信开发者工具

打开项目，应该能正常编译。

## 开始开发

现在一切就绪！在 Claude Code 中运行：

```
/plan

我要开发逻辑表达力小程序，今天是 Day 1，请帮我创建详细的实施计划。

核心功能：
1. 题库列表和详情
2. 答题功能
3. 学习进度追踪
4. 数据分析

技术栈：微信小程序 + TypeScript + 云开发

请按照 21 天开发计划指导我。
```

**祝您开发顺利！** 🎉
